// Automatically generated Godot externs: DO NOT EDIT
// MIT licensed, see LICENSE.md
package godot;

@:native("Godot.SpatialMaterial.TextureParam")
@:csNative
extern enum SpatialMaterial_TextureParam {
	/**		
		Texture specifying per-pixel color.
	**/
	Albedo;

	/**		
		Texture specifying per-pixel metallic value.
	**/
	Metallic;

	/**		
		Texture specifying per-pixel roughness value.
	**/
	Roughness;

	/**		
		Texture specifying per-pixel emission color.
	**/
	Emission;

	/**		
		Texture specifying per-pixel normal vector.
	**/
	Normal;

	/**		
		Texture specifying per-pixel rim value.
	**/
	Rim;

	/**		
		Texture specifying per-pixel clearcoat value.
	**/
	Clearcoat;

	/**		
		Texture specifying per-pixel flowmap direction for use with `godot.SpatialMaterial.anisotropy`.
	**/
	Flowmap;

	/**		
		Texture specifying per-pixel ambient occlusion value.
	**/
	AmbientOcclusion;

	/**		
		Texture specifying per-pixel depth.
	**/
	Depth;

	/**		
		Texture specifying per-pixel subsurface scattering.
	**/
	SubsurfaceScattering;

	/**		
		Texture specifying per-pixel transmission color.
	**/
	Transmission;

	/**		
		Texture specifying per-pixel refraction strength.
	**/
	Refraction;

	/**		
		Texture specifying per-pixel detail mask blending value.
	**/
	DetailMask;

	/**		
		Texture specifying per-pixel detail color.
	**/
	DetailAlbedo;

	/**		
		Texture specifying per-pixel detail normal.
	**/
	DetailNormal;

	/**		
		Represents the size of the `godot.SpatialMaterial_TextureParam` enum.
	**/
	Max;
}
